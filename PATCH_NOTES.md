# IP Racing — Stage 1.5 패치 노트

기존 저장소 위에 **파일 6개를 덮어쓰면** 적용됩니다. 서버/클라이언트 모두 `tsc --noEmit` 통과, `vite build` 성공 확인했습니다.

```
server/src/gameConfig.ts   ← 덮어쓰기
server/src/RaceRoom.ts     ← 덮어쓰기
server/src/schema.ts       ← 덮어쓰기
client/src/net.ts          ← 덮어쓰기
client/src/ui.ts           ← 덮어쓰기
client/src/RaceScene.ts    ← 덮어쓰기
client/index.html          ← 덮어쓰기
```

건드리지 않은 파일: `questions.ts`, `items.ts`, `index.ts`, `main.ts`, 배포 설정.

> **삭제 권장**: 저장소 루트의 `ui.ts`, `net.ts`는 `client/src/` 안의 파일과 **완전히 동일한 사본**입니다(diff 확인함). 빌드에 포함되지 않는 죽은 파일인데, 나중에 한쪽만 고치면 반드시 헷갈립니다.

---

## 1. 페이싱 재조정 — 가장 큰 체감 변화

**문제.** `trackLength 6000 / baseSpeed 40` = 완주 150초. 게이트가 6개니 퀴즈 사이 구간이 각 20~25초였습니다. 그 20초 동안 플레이어의 조작은 아이템 키 한 번뿐. 3분 게임에서 총 입력이 12회였습니다.

**수정.** `baseSpeed: 40 → 85` (완주 약 71초), 게이트 위치를 앞으로 당겨 구간을 9~12초로 압축. `simHz: 10 → 20`으로 보간 품질과 이펙트 만료 정밀도 개선.

트랙 길이는 그대로 뒀습니다. 렌더링 좌표계가 `progress / TRACK_LENGTH`라서 길이를 바꾸면 클라이언트 상수도 같이 고쳐야 하는데, 속도만 올리는 쪽이 되돌리기 쉽습니다.

## 2. 퀴즈가 레이스를 가리지 않게

**문제.** `.overlay { inset:0; background:rgba(8,12,24,.82) }` — 퀴즈 중 레이스가 전혀 안 보였습니다. 퀴즈와 레이스가 한 화면에 공존하는 순간이 0초. 두 재미가 서로를 가리고 있었습니다.

**수정.** 퀴즈만 하단 패널로 분리(로비·결과는 전체화면 유지). 이제 문제를 읽으면서 상대 차 위치가 보입니다. 결과도 즉시 닫지 않고 0.9초 유지 — 정답/오답 색 테두리를 읽을 시간을 줍니다.

## 3. 확신도 베팅 (신규 결정축)

**문제.** 퀴즈 레이싱의 근본 한계는 결정축이 "정답을 아느냐" 하나뿐이라는 것. 아는 사람은 이기고 모르는 사람은 아무것도 못 하며, 역전은 랜덤 아이템에만 의존합니다.

**수정.** READ 단계에서 이번 문제에 **안전 / 승부**를 겁니다. 지식 위에 **판단**이 얹힙니다.

| | 정답 | 오답 |
|---|---|---|
| 🛡 안전 | 아이템만 | ×0.7 / 2.5초 |
| 🔥 승부 | 아이템 + **×1.7 부스트 2.5초** | **×0.3 / 3.5초 (스핀아웃)** |

- 입력: 패널 버튼 클릭 또는 **Space** 토글 (Space는 레이스 중엔 기존대로 아이템 사용 — 페이즈로 분기)
- 기본값 `safe`. 아무것도 안 하면 손해 보지 않습니다.
- `choose_open` 시점에 잠금. 보기를 보고 나서 바꿀 수 없습니다.
- **상대 베팅이 보입니다.** "쟤가 승부를 걸었다"는 정보가 심리전을 만듭니다. 숨기고 싶으면 `schema.ts`에서 `PlayerState.bet` 필드만 빼면 됩니다.
- 결과 화면 IP 점수에 승부 성공 1건당 +3점 (상한 100).

밸런스는 전부 `CONFIG.BET`에 있습니다. 첫 테스트에서 `risk.wrongMultiplier`(0.3)가 너무 가혹한지부터 보세요.

## 4. 이펙트 스택 — 실제 버그 수정

**문제.** 기존 `applyEffect`는 배수를 덮어쓰고 타이머로 1.0 복귀시켰습니다. 두 가지 버그가 있었습니다.

- 로켓(2초) 맞은 직후 부스터(2초)를 쓰면, **로켓 타이머가 부스터를 조기 종료**시킵니다. 반격이 손해가 되는 상황.
- `clock.setTimeout`은 퀴즈 정지 중에도 흐릅니다. 방패 6초가 **퀴즈 보는 동안 소모**됐습니다.

**수정.** 효과를 `{tag, multiplier, remainMs}` 배열로 쌓고, 남은 시간은 **racing 틱에서만** 감소시킵니다. 최종 배수는 곱연산 — 로켓 0.4 × 부스터 1.5 = 0.6. 부스터가 로켓을 부분 상쇄하는, 직관에 맞는 결과가 됩니다. 같은 태그는 갱신되고 다른 태그끼리는 공존합니다.

## 5. 타격감

정답의 순간을 몸으로 느끼게 하는 레이어입니다. 파티클 100개보다 히트스톱 3줄이 체감이 큽니다.

- **히트스톱** — 정답 시 55ms(승부 성공 90ms) 시간 정지 → 화면 플래시 → 스쿼시&스트레치(1.35/0.7 → 1.0, `Back.easeOut`)
- **카메라** — 정답은 짧고 약하게(120ms/0.004), 오답은 길고 강하게(220ms/0.006), 승부 실패는 420ms/0.012 + 차량 360° 스핀. 피드백이 서로 구분돼야 합니다.
- **속도감** — 차선 대시가 선두 속도에 맞춰 스크롤, 배수 > 1.05일 때만 잔상 + 스피드라인. 항상 남기면 속도 대비가 사라집니다.
- **상태 가독성** — 감속 중 차체 알파 0.55, 내 차는 항상 위 레인 고정
- **결승선** — `timeScale` 1 → 0.25 슬로모션 + 화이트 플래시
- **이징** — 부스트 계열 `Back.easeOut`, 소멸 계열 `Quad.easeOut/In`. 선형 트윈은 전부 제거했습니다.

## 6. 이벤트 팬아웃 (잠재 버그 방지)

`RaceScene`과 `ui`가 이제 둘 다 `gate_resolved`를 구독합니다. colyseus.js 버전에 따라 `room.onMessage`를 같은 타입으로 두 번 걸면 콜백이 덮어써질 수 있어서, `net.on()`이 내부 배열로 팬아웃하도록 바꿨습니다. `room.onMessage`는 이벤트당 한 번만 겁니다.

---

## 테스트 체크리스트

브라우저 탭 2개로 붙어서 확인할 것:

1. 한쪽만 승부, 다른 쪽 안전 → 둘 다 정답. 승부 쪽이 눈에 띄게 앞서 나가는가?
2. 승부 걸고 일부러 오답 → 스핀아웃이 "억울하다"가 아니라 "내 판단 미스"로 느껴지는가?
3. 로켓 맞은 직후 부스터 → 배수 HUD가 0.6으로 뜨고 부스터가 제 시간을 다 쓰는가?
4. 방패 켜고 퀴즈 진입 → 퀴즈 끝난 뒤에도 방패가 살아 있는가?
5. 71초가 여전히 긴가, 짧은가? → `baseSpeed`만 조정

## 다음 후보 (이번엔 안 넣음)

- **슬립스트림** — 앞차 뒤에서 게이지 충전 → 추월 부스트. 순위 자체를 전략 자원으로 만들어 러버밴드를 "랜덤 보정"에서 "실력 기반 보정"으로 바꿉니다.
- **아이템 3중 1픽 드래프트** — 랜덤 지급을 선택으로. `items.ts`의 표시=획득 불일치 문제도 같이 해소됩니다.
- **3인 이상** — `maxClients`, `raceScoreByRank`, 레인 배치, 로켓 타겟팅이 전부 2인 가정입니다.
